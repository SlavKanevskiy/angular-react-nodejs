services:
  postgres:
    image: postgres:15-alpine
    container_name: locations_postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: locations_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./nest/database/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./nest
      dockerfile: Dockerfile
    container_name: locations_backend
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/locations_db
    volumes:
      - ./nest/src:/app/src
      - ./shared:/shared
    depends_on:
      postgres:
        condition: service_healthy
    command: yarn start:dev

  shell:
    build:
      context: .
      dockerfile: main/Dockerfile
    container_name: locations_shell
    restart: always
    ports:
      - "8080:8080"
    depends_on:
      - backend

volumes:
  postgres_data:

